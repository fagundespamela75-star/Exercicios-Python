# Vamos usar uma variavel chamada nome para
# Guardar o nome do cliente. Utilizaremos tambem
# O comando input(in -> dentro | put -> por em algum lugar)
nome = input("Digite o seu nome: ")
print("Olá, Sr(a)."+nome)
print(f"Olá, Sr(a). {nome}")  

  
    #não é necessario colocar quantidade de letras 
    #Sempre apertar o play para o terminal funcionar

    # O operador +(mais) 
    # foi utilizado para concatenar (juntar)
    # o texto entre aspas ("") com a variavel nome

    # A baixo, usamos o comando print com a letra
    # f(format) paea inserir uma variavel no meio 
    # de uma string. A variavel foi inserida com chaves ({})
    # esta tecnica é chamada de interpolação

print("Olá, Sr(a)."+nome+". Seja bem vindo")
print(f"Olá, Sr(a). {nome}. Seja bem vindo") 
